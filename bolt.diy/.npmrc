# Enforce exact versions for new dependencies
save-exact=true

# Prevent accidental updates
save-prefix=''

# Require lockfile to be present
lockfile=true

# Frozen lockfile in CI (prevents auto-updates)
# Uncomment for CI environments:
# frozen-lockfile=true
